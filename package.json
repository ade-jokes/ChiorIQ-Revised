{
  "name": "ChiorIQ",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "member-app",
    "leader-app",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev:frontend",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:lead":"npm run dev:leader",
    "dev:leader": "npm run dev --workspace leader-app",
    "dev:member": "npm run dev --workspace member-app",
    "dev:member-app": "npm run dev --workspace member-app",
    "dev:leader-app": "npm run dev --workspace leader-app",
    "build:frontend": "npm run build --workspace frontend",
    "build:member": "npm run build --workspace member-app",
    "build:leader": "npm run build --workspace leader-app",
    "test": "npm run test --workspace backend",
    "test:backend": "npm run test --workspace backend"
  }
}

